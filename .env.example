# Variables de entorno para la aplicación UNSAAC Horarios

# Base de datos PostgreSQL (Supabase)
# URL de conexión principal con pooling para aplicaciones
DATABASE_URL=postgresql://postgres.username:password@host:6543/postgres?pgbouncer=true

# URL de conexión directa para migraciones y operaciones que requieren conexión directa
DIRECT_URL=postgresql://postgres.username:password@host:5432/postgres

# Instrucciones:
# 1. Copia este archivo como .env en la raíz del proyecto
# 2. Reemplaza 'username', 'password' y 'host' con tus credenciales reales de Supabase
# 3. Para despliegue en Vercel, configura estas variables en el panel de Vercel:
#    - Ve a tu proyecto en Vercel Dashboard
#    - Navega a Settings > Environment Variables
#    - Agrega DATABASE_URL y DIRECT_URL con los valores correspondientes
#    - Asegúrate de seleccionar todos los entornos (Production, Preview, Development)