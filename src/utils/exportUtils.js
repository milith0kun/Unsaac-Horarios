// Utilidades para exportar horarios como imagen y PDF

/**
 * Exporta el horario como imagen
 * @param {Array} _cursos - Array de cursos para exportar (no utilizado actualmente)
 */
// eslint-disable-next-line no-unused-vars
export const exportarComoImagen = async (_cursos) => {
  try {
    // Funcionalidad temporalmente deshabilitada
    alert('Funcionalidad de exportar imagen temporalmente no disponible.');
    return false;
  } catch (error) {
    console.error('Error al exportar como imagen:', error);
    return false;
  }
};

/**
 * Exporta el horario como PDF
 * @param {Array} _cursos - Array de cursos para exportar (no utilizado actualmente)
 */
// eslint-disable-next-line no-unused-vars
export const exportarComoPDF = async (_cursos) => {
  try {
    // Funcionalidad temporalmente deshabilitada
    alert('Funcionalidad de exportar PDF temporalmente no disponible.');
    return false;
  } catch (error) {
    console.error('Error al exportar como PDF:', error);
    return false;
  }
};